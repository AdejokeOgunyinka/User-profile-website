import { HeaderStyle, Heading, HeaderText } from './style';

const Header = () => {
	return (
		<HeaderStyle>
			<Heading>Enye User Profiles</Heading>
			<HeaderText>A collective list of user data on the enye records API.</HeaderText>
		</HeaderStyle>
	);
};

export default Header;
